import { Bayon, Port_Lligat_Sans } from 'next/font/google';

export const bayon = Bayon({
	weight: '400',
	subsets: ['latin'],
	variable: '--font-display',
});
export const portLligatSans = Port_Lligat_Sans({
	weight: '400',
	subsets: ['latin'],
	variable: '--font-body',
});
